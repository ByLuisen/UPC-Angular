import { RepetirPasswordDirective } from './repetir-password.directive';

describe('RepetirPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new RepetirPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
